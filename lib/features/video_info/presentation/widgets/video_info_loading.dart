import 'package:flutter/material.dart';

class VideoInfoLoading extends StatelessWidget {
  const VideoInfoLoading({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(child: CircularProgressIndicator());
  }
}
